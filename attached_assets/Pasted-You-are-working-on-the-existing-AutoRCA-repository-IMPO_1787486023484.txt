You are working on the existing AutoRCA repository.

IMPORTANT:
Do NOT create demo data, fake logs, hardcoded traceback strings, simulated incidents, mock evidence, or a new toy example.

I want to validate the EXISTING AutoRCA system using a REAL locally running application and a REAL failure.

First, inspect the entire repository and understand the existing architecture before changing anything.

The existing deterministic flow is:

Real Application / Real Failure
        ↓
Real source data
        ↓
Collectors
        ↓
Extractors
        ↓
Observations
        ↓
Evidence
        ↓
RuleEngine
        ↓
Hypotheses
        ↓
Best Root Cause
        ↓
RCARequestBuilder
        ↓
Structured RCARequest
        ↓
LLM
        ↓
FinalRCA
        ↓
FinalRCAValidator
        ↓
Final Incident Report

The deterministic core MUST remain unchanged unless a real integration bug is discovered.

CURRENT PROJECT STATUS:
- RulesConfig exists.
- AnalysisPipeline exists.
- RuleEngine exists.
- ScoringEngine exists.
- EvidenceBuilder exists.
- RCARequestBuilder exists.
- FinalRCAValidator exists.
- Real Git collector exists.
- Real file collector exists.
- CLI exists.
- Schema validation exists.
- Existing tests are currently passing.
- The project previously reached 57/57 tests before the latest LLM/UI work.
- Real Git commits and real Python tracebacks were already tested successfully.
- The real incident previously produced 90% evidence-based confidence.
- Do NOT replace this architecture with a simpler implementation.

==================================================
PHASE 1 — REAL INCIDENT TEST
==================================================

Before integrating anything else, prove that AutoRCA can analyze a REAL failure.

Create/use a small REAL application inside a dedicated test workspace ONLY if necessary.

The application must actually run and fail because of a real configuration/dependency/network problem.

Do NOT write the failure traceback manually.

The traceback/log must be generated by actually running the application.

Do NOT hardcode the expected AutoRCA result into the application or CLI.

The incident source data must come from real execution.

Prefer testing these three real failure types separately:

1. Missing Environment Variable
2. Missing Dependency
3. Port Conflict

For each scenario:

A. Create/run an actual application.
B. Cause the failure naturally.
C. Capture the actual output/log/traceback.
D. If applicable, create a real Git commit containing the configuration/code change that caused the incident.
E. Run the EXISTING AutoRCA CLI against those real sources.
F. Show the actual AutoRCA result.

The system must determine the root cause from the collected evidence.

==================================================
PHASE 2 — NO DEMO DATA
==================================================

Search the repository for:

- hardcoded traceback strings
- hardcoded log strings
- hardcoded evidence
- hardcoded root causes
- hardcoded confidence values
- fake Git SHAs
- fake incident IDs
- mock RCA results
- deterministic placeholder data being passed as if it were real incident data

Do NOT remove legitimate test fixtures.

Tests may contain fixtures/mocked data because they are tests.

But production/demo execution paths MUST NOT depend on those fixtures.

Clearly separate:

tests/
    test fixtures are allowed

production/runtime path/
    must use real collected data

If the current CLI still depends on manually supplied text files, improve it only where necessary so the input comes from an actual running application/log source.

Do not redesign the Core.

==================================================
PHASE 3 — REAL LLM INTEGRATION
==================================================

Only after the real deterministic incident flow works, implement:

PipelineResult
      ↓
RCARequestBuilder
      ↓
Structured RCARequest
      ↓
LLM
      ↓
FinalRCA
      ↓
FinalRCAValidator

CRITICAL SECURITY/ARCHITECTURE RULE:

The LLM MUST NOT receive raw logs and independently determine the root cause.

The LLM receives ONLY the structured RCARequest generated by RCARequestBuilder.

The deterministic engine remains responsible for:

- evidence
- scoring
- hypotheses
- selected root cause
- confidence
- severity
- evidence IDs
- excluded hypotheses
- generation options

The LLM is responsible only for:

- human-readable explanation
- recommended fix
- optionally generating a PR suggestion according to the existing schema/design

Then FinalRCAValidator MUST verify the LLM response.

If the LLM attempts to change:

- hypothesis_id
- confidence
- cited evidence
- resolved severity
- generation options

the validator must reject the response.

Do NOT allow the LLM to become the decision maker.

==================================================
PHASE 4 — REAL LLM PROVIDER
==================================================

Do not hardcode an API key.

Use environment variables.

Example:

LLM_API_KEY
LLM_MODEL
LLM_BASE_URL

If the repository already has an LLM provider abstraction, use it.

If not, create a minimal provider abstraction without changing the deterministic architecture.

The application must fail clearly if the required LLM configuration is missing.

Do not silently fall back to fake/generated responses.

Do NOT create a fake LLM implementation for the production path.

==================================================
PHASE 5 — REAL END-TO-END TEST
==================================================

Run one complete real incident:

REAL APPLICATION
→ REAL FAILURE
→ REAL LOG/TRACEBACK
→ REAL GIT CHANGE if applicable
→ REAL COLLECTORS
→ REAL OBSERVATIONS
→ REAL EVIDENCE
→ REAL RULE ENGINE
→ REAL ROOT CAUSE
→ RCARequest
→ REAL LLM
→ FinalRCA
→ FinalRCAValidator
→ Final Incident Report

Print/log every important stage so I can inspect it.

I want to see:

1. What the application actually failed with.
2. What the collectors actually collected.
3. What observations were extracted.
4. What evidence was generated.
5. What hypothesis was selected.
6. The confidence.
7. The RCARequest sent to the LLM.

IMPORTANT:
Never print or expose the LLM API key.

8. The FinalRCA returned by the LLM.
9. Whether FinalRCAValidator accepted or rejected it.
10. The final human-readable incident report.

==================================================
PHASE 6 — REAL WEB DEMO UI
==================================================

After the backend flow works, build a minimal professional web UI.

The UI should display REAL results from the running AutoRCA pipeline.

Do NOT populate the UI with static/demo values.

Example:

AutoRCA
Evidence-Based Root Cause Analysis

Incident: <REAL INCIDENT ID>
Environment: Production

Root Cause
Missing Environment Variable

Confidence
90%

Evidence
✓ PORT removed from .env
✓ KeyError: PORT

Severity
CRITICAL

Related Change
<REAL COMMIT SHA>

AI Explanation
<REAL LLM RESPONSE>

Recommended Fix
<REAL LLM RESPONSE>

The UI must call the backend and retrieve the actual analysis result.

No hardcoded:
90%
Missing Environment Variable
PORT
fake commit SHA
fake evidence

Those values must come from the actual pipeline response.

==================================================
PHASE 7 — TESTING
==================================================

After implementation:

1. Run ALL existing tests.
2. Add tests for the new LLM integration.
3. Add tests for FinalRCAValidator.
4. Add tests ensuring the LLM only receives RCARequest.
5. Add an integration test for the complete flow.
6. Verify the existing deterministic tests still pass.
7. Verify the real application incident manually.

Do not delete existing tests just to make the suite pass.

Do not weaken assertions.

==================================================
PHASE 8 — REPORT THE TRUTH
==================================================

At the end, give me a clear report with:

A. What was already implemented.
B. What you changed.
C. What is genuinely real.
D. What is still mocked/test-only.
E. Exact command used to run the real incident.
F. Exact command used to start the web UI.
G. Test count and result.
H. Whether the complete flow actually reached a real LLM.
I. Whether FinalRCAValidator accepted the response.
J. Any remaining Technical Debt.

IMPORTANT:

If something cannot be made genuinely real in the Replit environment, DO NOT fake it.

Tell me exactly what is blocking it and why.

The goal is NOT to make the demo look real.

The goal is to make AutoRCA actually work on a real incident.

Do not rewrite the architecture.
Do not remove the deterministic reasoning layer.
Do not move root-cause decision making into the LLM.
Do not introduce fake production data.
Do not claim an integration works unless you actually execute it.